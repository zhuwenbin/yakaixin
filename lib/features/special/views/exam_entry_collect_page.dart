import 'package:flutter/material.dart';

/// P8-3 考点收藏 - 收藏考点列表
class ExamEntryCollectPage extends StatelessWidget {
  const ExamEntryCollectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('考点收藏')),
      body: const Center(child: Text('考点收藏页面')),
    );
  }
}
