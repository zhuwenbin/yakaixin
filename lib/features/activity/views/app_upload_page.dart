import 'package:flutter/material.dart';

/// P9-2 APP下载 - APP下载页
class AppUploadPage extends StatelessWidget {
  const AppUploadPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('APP下载')),
      body: const Center(child: Text('APP下载页面')),
    );
  }
}
