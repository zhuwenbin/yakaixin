import 'package:flutter/material.dart';

/// P3-1 全部模考 - 模考列表
class ModelExamIndexPage extends StatelessWidget {
  const ModelExamIndexPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('全部模考')),
      body: const Center(child: Text('全部模考页面')),
    );
  }
}
