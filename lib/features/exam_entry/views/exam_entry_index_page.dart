import 'package:flutter/material.dart';

/// P2-14 考点词条 - 考点列表
class ExamEntryIndexPage extends StatelessWidget {
  const ExamEntryIndexPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('考点词条')),
      body: const Center(child: Text('考点词条页面')),
    );
  }
}
