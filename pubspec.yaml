name: yakaixin_app
description: "牙开心学堂 - Flutter版"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # 状态管理
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3
  
  # 网络请求
  dio: ^5.4.0
  
  # 路由
  go_router: ^17.0.0
  
  # 本地存储
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # UI相关
  flutter_screenutil: ^5.9.0
  cached_network_image: ^3.3.1
  pull_to_refresh: ^2.0.0
  flutter_easyloading: ^3.0.5
  fluttertoast: ^8.2.4
  carousel_slider: ^5.0.0
  table_calendar: ^3.0.9
  
  # 媒体播放
  # ⚠️ 临时注释：测试是否与百家云SDK冲突
  # video_player: ^2.8.2
  # chewie: ^1.7.5
  # audioplayers: ^5.2.1
  
  # PDF查看
  flutter_pdfview: ^1.3.2
  
  # 图表
  fl_chart: ^0.66.0
  
  # HTML渲染
  flutter_html: ^3.0.0-beta.2
  
  # 二维码
  qr_flutter: ^4.1.0
  
  # 分享功能
  share_plus: ^7.2.2
  
  # 微信支付SDK
  fluwx: ^5.7.5
  
  # iOS内购
  in_app_purchase: ^3.2.0
  in_app_purchase_storekit: ^0.3.6+7
  
  # 百家云直播SDK
  # bjy_liveui_flutter: ^4.16.0
  # bjy_liveui_flutter: ^4.15.3
  
  # WebView（用于H5直播）
  webview_flutter: ^4.10.0
  
  # 视频播放器（使用 Better Player，更好的性能和鸿蒙系统支持）
  better_player: ^0.0.84

  # 工具
  json_annotation: ^4.9.0
  freezed_annotation: ^2.4.1
  logger: ^2.0.2+1
  intl: ^0.20.2
  crypto: ^3.0.3
  url_launcher: ^6.2.4
  
  # 图片选择
  image_picker: ^1.0.7
  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  
  # 代码生成
  build_runner: ^2.4.8
  riverpod_generator: ^2.3.9
  json_serializable: ^6.7.1
  freezed: ^2.4.6
  hive_generator: ^2.0.1
  
  # 应用图标和启动页配置
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.3.10

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/course_type/
    - assets/images/style/
    - assets/images/secret.png
    # ✅ Mock 数据文件（仅开发环境使用）
    - assets/mock/
    - assets/config/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# ============ 应用图标配置 ============
flutter_launcher_icons:
  android: true
  ios: true
  # iOS 和 Android 普通图标
  image_path: "assets/images/app_icon.png"
  
  # Android 自适应图标配置（Android 8.0+）
  # ✅ 如果图标被裁剪，需要使用以下两种方案之一：
  # 
  # 方案 1：使用表态图（如果 app_icon.png 是带背景的完整图标）
  adaptive_icon_background: "assets/images/app_icon.png"  # 背景使用完整图标
  # adaptive_icon_foreground 不设置，只显示背景
  # 
  # 方案 2：使用纯色背景 + 前景图（需要重新设计图标）
  # adaptive_icon_background: "#FFFFFF"  # 背景色
  # adaptive_icon_foreground: "assets/images/app_icon_foreground.png"  # 前景图（留安全边距）
  
  # iOS 配置
  remove_alpha_ios: true  # 移除透明通道

# ============ 启动页配置 ============
flutter_native_splash:
  # ✅ 使用全屏背景图（充满屏幕）
  background_image: "assets/images/splash.png"
  
  # 深色模式背景图
  background_image_dark: "assets/images/splash.png"
  
  # Android 12+ 配置（仍然需要居中图标）
  android_12:
    image: "assets/images/app_icon.png"
    color: "#E8F4F8"
    icon_background_color: "#E8F4F8"
  
  ios: true
  android: true
  web: false
