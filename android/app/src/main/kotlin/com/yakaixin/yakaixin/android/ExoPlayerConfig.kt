package com.yakaixin.yakaixin.android

/**
 * 占位实现（暂未使用自定义 RenderersFactory）。
 *
 * 说明：
 * - 之前的实现依赖旧版 ExoPlayer API，导致编译期无法解析。
 * - 当前工程未实际调用该配置类，故提供最小占位以确保编译通过。
 * - 如需自定义 ExoPlayer RenderersFactory，请在引入对应依赖后补充实现。
 */
object ExoPlayerConfig

