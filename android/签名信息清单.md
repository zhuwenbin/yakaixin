# Android 应用签名信息清单

## 📦 应用基本信息

- **包名（Package Name）**：`com.jinyisheng.jinyisheng.android`
- **应用名称**：金医圣

---

## 🔐 正式签名（Release Signing）

### 签名文件信息
- **密钥库文件**：`android/release.jks`
- **密钥别名**：`upload`
- **创建日期**：2026年4月21日
- **有效期**：2026年4月21日 - 2053年9月6日

### 证书信息
- **所有者**：CN=Jinyisheng, OU=Jinyisheng, O=Jinyisheng, L=Beijing, ST=Beijing, C=CN
- **签名算法**：SHA384withRSA
- **密钥算法**：2048 位 RSA 密钥

### 证书指纹

#### MD5 指纹（用于备案）
```
14:C5:46:B4:E2:3F:09:F8:49:12:AD:D4:D9:DB:86:41
```

**去除冒号格式（如需要）**：
```
14C546B4E23F09F84912ADD4D9DB8641
```

#### SHA1 指纹
```
72:DC:D5:37:0F:60:94:24:A0:C2:A9:90:35:3F:07:04:F7:BC:F9:AD
```

#### SHA256 指纹
```
FC:8F:E6:2C:52:7D:21:9A:4C:C3:4A:75:5B:8B:8D:9B:F2:DF:8A:C5:A8:5B:52:05:39:71:A3:F7:4C:C8:BB:09
```

---

## 🔧 备用签名（Debug Signing）

### 签名文件信息
- **密钥库文件**：`~/.android/debug.keystore`（系统默认）
- **密钥别名**：`androiddebugkey`
- **创建日期**：2025年11月17日
- **有效期**：2025年11月17日 - 2055年11月10日

### 证书信息
- **所有者**：C=US, O=Android, CN=Android Debug
- **签名算法**：SHA256withRSA
- **密钥算法**：2048 位 RSA 密钥

### 证书指纹

#### MD5 指纹
```
5D:56:DA:A9:8F:6A:D1:71:96:16:32:F2:4C:53:BE:3C
```

**去除冒号格式（如需要）**：
```
5D56DAA98F6AD171961632F24C53BE3C
```

#### SHA1 指纹
```
F5:00:BF:A8:40:AB:24:29:B7:8B:59:18:20:1C:78:0F:B3:DB:24:15
```

#### SHA256 指纹
```
C5:F9:5A:AD:27:E0:EC:3C:6C:DE:BA:50:2C:2D:47:CE:6F:AE:3F:FA:9E:40:AA:A4:F5:0E:EE:3F:1C:8E:D8:D7
```

---

## 📋 备案填写信息（正式签名）

### 安卓平台软件包名称
```
com.jinyisheng.jinyisheng.android
```

### 证书MD5指纹
```
14:C5:46:B4:E2:3F:09:F8:49:12:AD:D4:D9:DB:86:41
```

**或去除冒号格式**：
```
14C546B4E23F09F84912ADD4D9DB8641
```

---

## 🔄 验证命令

### 查看正式签名信息
```bash
cd android
keytool -list -v -keystore release.jks -alias upload -storepass Jys2025Release! -keypass Jys2025Release!
```

### 查看备用签名信息
```bash
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
```

### 获取MD5指纹
```bash
# 正式签名
keytool -exportcert -alias upload -keystore android/release.jks -storepass Jys2025Release! -keypass Jys2025Release! -file /tmp/cert.cer
openssl x509 -inform DER -in /tmp/cert.cer -noout -fingerprint -md5

# 备用签名
keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore -storepass android -keypass android -file /tmp/debug_cert.cer
openssl x509 -inform DER -in /tmp/debug_cert.cer -noout -fingerprint -md5
```

---

## ⚠️ 注意事项

1. **正式签名用于发布**：正式签名（release.jks）用于生产环境发布
2. **备用签名用于开发**：Debug签名仅用于开发和测试
3. **备案使用正式签名**：应用备案时请使用正式签名的信息
4. **MD5格式**：备案系统可能需要去除冒号的格式，请根据系统要求选择
5. **签名密码**：Jys2025Release!

---

## 📝 更新记录

- **创建日期**：2025-01-25
- **正式签名更新日期**：2026-04-21
- **备用签名创建日期**：2025-11-17

